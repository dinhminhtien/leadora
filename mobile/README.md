# Leadora Mobile

Reserved workspace for the Flutter mobile sales app.

Suggested first-level layout when the app is scaffolded:

```text
assets/
lib/
  core/
  routes/
  features/
  state/
  data/
```

Keep screens and feature workflows inside `features/`. API clients, repositories, models, and local storage belong in `data/`.
