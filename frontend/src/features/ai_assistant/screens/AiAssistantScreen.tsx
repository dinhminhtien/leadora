import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export function AiAssistantScreen() {
  return (
    <ModulePlaceholder
      title="AI Assistant"
      description="Support staff with workflow-aware suggestions and customer follow-up context."
      cardTitle="Assistant workspace"
      cardDescription="Chat sessions, message history, and suggested actions can be connected here."
    />
  );
}
