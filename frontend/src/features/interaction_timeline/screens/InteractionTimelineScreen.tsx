import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export function InteractionTimelineScreen() {
  return (
    <ModulePlaceholder
      title="Interaction Timeline"
      description="Review calls, messages, notes, and sales touchpoints in chronological order."
      cardTitle="Timeline stream"
      cardDescription="Interactions can be grouped by lead, customer, or deal when the API is connected."
    />
  );
}
