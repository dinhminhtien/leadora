import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export function NotificationListScreen() {
  return (
    <ModulePlaceholder
      title="Notifications"
      description="Surface important workflow updates for leads, tasks, handovers, and bookings."
      cardTitle="Notification center"
      cardDescription="Unread state, filters, and priority alerts can be added here."
    />
  );
}
