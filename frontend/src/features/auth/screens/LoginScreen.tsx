import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export function LoginScreen() {
  return (
    <ModulePlaceholder
      title="Login"
      description="Sign in to access Leadora workflow management."
      cardTitle="Authentication entry point"
      cardDescription="This screen is ready for React Hook Form and Zod validation when the backend contract is connected."
    />
  );
}
