import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export function ForgotPasswordScreen() {
  return (
    <ModulePlaceholder
      title="Forgot Password"
      description="Start the password recovery workflow for hotel staff."
      cardTitle="Recovery workflow"
      cardDescription="This placeholder can be connected to the authentication service password reset endpoint."
    />
  );
}
