import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export function DealListScreen() {
  return (
    <ModulePlaceholder
      title="Deals"
      description="Manage active deals, values, owners, and commercial status."
      cardTitle="Deal register"
      cardDescription="This area is ready for deal tables, status changes, and revenue tracking."
    />
  );
}
