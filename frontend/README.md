# Leadora Frontend

Reserved workspace for the Next.js web dashboard.

Suggested first-level layout when the app is scaffolded:

```text
app/
shared/
features/
services/
stores/
assets/
```

Keep feature-specific UI, hooks, and state inside `features/`. Shared primitives belong in `shared/`; backend API wrappers belong in `services/`.
